interface:
  display_name: "Revit Mcp"
  short_description: "Repo-specific patterns and workflows for revit-mcp"
  default_prompt: "Use the revit-mcp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true